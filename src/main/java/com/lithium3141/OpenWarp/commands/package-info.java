/**
 * Package containing commands to be run by the OpenWarp plugin. Each class
 * in this package is expected to be a subclass of the OWCommand class, and
 * have the appropriate methods implemented such that it represents a
 * runnable command for an installed OpenWarp instance.
 */
package com.lithium3141.OpenWarp.commands;
